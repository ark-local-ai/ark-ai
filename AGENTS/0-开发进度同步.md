# 0 · 开发进度同步（Agent 架构详解）

> 本项目的「最新进度」唯一权威入口。**每完成一步就回来更新本文件**。
> 回答三件事：现在做到哪、正在做什么、下一步做什么。配套各文详见文末索引。

---

## 一、当前进度总览（更新至 2026-09-11）

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M0 前端壳完整 | 官网 + 工作台 10 页，mock 数据可跑 | ✅ 既有 |
| M1 后端骨架 | Node+TS+Fastify+node:sqlite，健康检查 | ✅ |
| M2 任务真闭环 | POST /api/tasks → 拆步 → SSE → 交付下载 | ✅ 已接前端 |
| M3 真实 LLM planner | openai 兼容拆真步骤，无 key 降级脚本计划器 | ✅ |
| M4 真实 Office 交付 | PPT/Excel/Word 可编辑真实文件（pptxgenjs/exceljs/docx）| ✅ **E2E 三场景验证** |
| M4b 模型 router + verifier | 多渠道按成功率选优（router）；交付文件校验+限次重试（verifier）| ✅ |
| M5 对话流式 | POST /api/chat SSE 流式（真实 LLM / 内置降级）+ 前端 Chat 页接 | ✅ |
| M6 专家/技能/自动化 | 技能 Markdown 落盘热加载 CRUD + 专家 API + 自动化 Cron 定时任务 | ✅ |
| B1 模型渠道 CRUD | DB 持久化渠道（增删改/设默认/验活），与 router 打通 | ✅ |
| B3 连接器状态 | GET /api/connectors 真实状态（渠道计数 + Ollama 探测）| ✅ |
| M7 工作空间真扫描 | 真目录扫描（GET /api/workspace）+ 前端列表/下载/刷新 | ✅ |
| M8 桌面封装 | Tauri 2 复用前端 | 🔲 交给小伙伴封装 |
| M9 真实场景库/接真列表 | 场景库 GET /api/scenarios + 首页最近任务/侧栏接真实 backend | ✅ |
| M10 工具注册表 + 逐步真执行 | tools/registry.ts（{name,describe,run}）+ orchestrator 每步经工具产出内容、注入交付 | ✅ |
| M10a LLM 真内容 | tools/llmContent.ts 用已配模型生成交付正文，无门控降级脚本（后由 M10b 取代） | ✅ |
| M10b 多 Agent 管线 | agent/pipeline.ts 分析→写手→校验三角色，LLM 优先+降级；删除 llmContent.ts | ✅ |
| M11 多工作空间 | spaces 表 + CRUD + 活动空间 + 交付写活动空间目录；前端空间视图接真 | ✅ |
| M12 用户/登录 | users/sessions 表（scrypt）、auth 路由、前端登录/注册/当前用户 | ✅ |
| M13 多用户隔离 | tasks/spaces 加 user_id，路由按当前用户过滤/绑定，前端带 token | ✅ |
| M14 多渠道 failover | router.chatWithFailover 按成功率逐个试所有渠道，全败才降级；超时保护 | ✅ |
| M15 成本/延迟/优先级加权 | channels 加 priority/cost、channel_stats 加延迟；复合评分=可靠性50%+延迟20%+成本15%+优先级15% | ✅ |
| M16 任务串行队列 + 失败兜底 | runner.ts 模块级 Promise 链 queue（同时只跑一个）；异常→failed+SSE error，不再卡 running | ✅ |
| M17 任务生命周期 | POST /:id/retry（幂等复位重跑）+ DELETE /:id（含步骤产物）；TaskPage 加按钮 | ✅ |
| M18 统计仪表盘 | GET /api/stats + 前端 /app/stats（任务吞吐/渠道健康/最近动态） | ✅ |
| M19 对话持久化 | chat_sessions/chat_messages 两表 + ?sess= 恢复 + 侧栏会话栏 | ✅ |
| M20 工作空间全文搜索 | FTS5 trigram 文件名索引 + GET /api/workspace/search + Cmd+K 搜文件 | ✅ |
| M21 全局任务事件/通知 | GET /api/events 全局 SSE 扇出 + 浏览器通知 + 侧栏 done/error 自动刷新 | ✅ |
| M22 任务历史归档 | tasks.archived 列 + /:id/archive + 侧栏归档切换 + 右键/TaskPage 归档按钮 | ✅ |
| M23 对话深度搜索 | chat_messages FTS5 trigram 索引 + GET /api/chat/search + Cmd+K「历史对话」区 | ✅ |
| M24 进程重启任务恢复 | getUnfinishedTasks + resumeUnfinishedTasks 启动续跑（resetTask 幂等）| ✅ |
| M25 自动化测试 | vitest 套件（ARK_DB_PATH/ARK_TEST 隔离临时库）store/events/runner/HTTP 26→31 用例 | ✅ |
| M26 结构化日志 | util/log.ts pino（service 常驻 + taskId 贯穿）+ orchestrator/runner 打点 | ✅ |
| M27 多渠道熔断器 | router 每渠道熔断（5 次 open + 10s cooldown + half-open 探测）+ 单测 | ✅ |
| M28 验收回归评测集 | test/acceptance.test.ts：把「一句话→交付可编辑 Office」核心卖点固化为回归断言（PPT/Excel/Word 类型映射 + 真实生成/合法 zip/ExcelJS 读回 + verifier 重试 + defaultTool），12 用例 | ✅ |
| M29 操作审计日志 | audit_log 表 + 根 onResponse 钩子统一审计 POST/PUT/DELETE（登录/建任务/删渠道…，当前用户/匿名），GET /api/audit 可查 | ✅ 4 单测（store 3 + http 1），全量 47 用例 |
| M30 任务并发控制 | runner 从严格串行改信号量受控并发（默认 2，ARK_MAX_CONCURRENCY 覆盖，FIFO 排队）+ GET /api/queue 队列状态 + 统计页队列卡 | ✅ 2 单测（并发上限+并行、stats active/queued、env=1 严格串行），全量 49 用例 |
| M31 逐步断点续跑 | resumeTask 复用已持久化步骤（note 存产出），从第一个未完成步续跑、不 reset；runner.resumeUnfinishedTasks 改走 resumeTask | ✅ 2 单测（有断点续跑不重跑已完步、无断点退化为全新），全量 51 用例 |
| M32 自动清理策略 | tasks.created_ts（epoch ms）+ 按龄清理终态(done/failed)非归档任务及其交付文件 + 审计日志裁剪；`POST /api/maintenance/cleanup` 手动触发 + 调度器每 6h 自动（env 可配 ARK_CLEANUP_ENABLED/TASK_DAYS/AUDIT_MAX） | ✅ 5 store 单测 + 1 http 端点测试，全量 57 用例 |
| M33 任务模板 | 模板 CRUD + 一键应用模板建任务（预填提示词/专家/技能/模型） | ✅ 5 store 单测 + 2 http 端点测试（含应用模板建任务），全量 64 用例 |
| M34 任务另存为模板 | done 任务一键固化为模板（继承 prompt/expert/skills/model）；任务页「另存为模板」按钮 | ✅ 1 http 端点测试，全量 65 用例 |
| M35 模板字段真正生效 | 应用模板时 expert/skills 落进任务、model 作为 modelHint 偏好渠道路由（非排他） | ✅ 2 orchestrator 单测（带/不带 opts），全量 67 用例 |
| M36 认证硬门禁 | 写请求（POST/PUT/PATCH/DELETE→/api/*）需登录 401，读开放；`/api/auth/*` 自管；`ARK_REQUIRE_AUTH=0` 可回退软门禁 | ✅ 1 http 专用 gate 测试，全量 68 用例 |
| M37 定时任务可跑模板 | 自动化 job 的 action 支持 `template:<id>`（复用模板及其 expert/skills/model）；前端新建表单加「跑模板」下拉 | ✅ 3 jobs 单测（resolveAction），全量 71 用例 |
| M38 交付版本历史 | 每次产出交付快照进 `_ark_versions/<name>/` + `file_versions` 表；`/api/versions` 列表 + 回滚；工作空间页「版本」面板 | ✅ 3 store + 3 fileVersions 工具 + 2 http，全量 79 用例 |
| M39 Agent 角色可配置 | 专家由「静态内置」→「内置 + 用户自建落库 CRUD（experts 表，多用户隔离）」；技能页新增「新建 + 删除」（PUT 即可 upsert，DELETE 删 .md，加 id 防穿越）；专家页「创建专属专家」真正可用 | ✅ 3 store 单测 + 2 http 端点测试，全量 84 用例 |
| M40 对话式多轮任务执行 | 对话不再只是聊天——`POST /api/chat` 命中工作意图（`runTask` 或动作+产出物检测）时把该消息入队成真实 Agent 任务，SSE 回传 `task_created`；前端「执行」开关订阅任务 SSE，气泡内实时进度 + 交付文件下载 | ✅ 2 chat 单测（looksLikeTask），全量 86 用例 |
| M41 记忆系统 || M42 记忆注入对话+`/记得` || M43 内置网页读取工具 || M44 内置知识检索工具 || M45 IM 消息桥 || M46 Web/检索工具接入编排管线 | `tools/context.ts` gatherContext：任务提示词内网址→web.read 读正文、本地知识→search.knowledge 检索；注入 analyst/writer 的 LLM 提示 + 追加「参考资料」section 进交付文件（docx 渲染 overflow section）；`runContentPipeline` 带 userId 多用户隔离；`reindexWorkspace` 从导入副作用改为 server 启动显式调用 | ✅ 3 context 单测（extractUrls/appendRefSection/gatherContext 隔离）+ 管线单测验证产出参考资料 section + E2E（docx 含参考资料/本地资料/记忆内容），全量 110 用例 |
| M47 内置联网搜索工具 | `tools/websearch.ts` search.web——本地优先、默认免 key 走 DuckDuckGo HTML（`result__title` 拆块 + `result__a`/`result__snippet` 正则 + `uddg` 解码真实链接），可选 `ARK_WEBSEARCH_ENDPOINT/KEY` 接自配 provider；注册进 `/api/tools` + `POST /api/tools/web.search`（缺 query 400）；接入 `gatherContext`：未给网址且命中调研/搜索意图（调研/竞品/市场/最新/趋势…）自动联网取回标题+摘要，注入多 Agent 上下文并追加「参考资料」section（失败静默降级） | ✅ 4 websearch 单测（解析/默认免 key 成功/HTTP 500/空 query）+ 真实 HTML E2E（8 条命中、清华/央视/百度百科标题与真 URL 解码正确）+ tools 路由注入验证（成功 200/空 400/清单含 web.search/auth 401），全量 114 用例 |
| M48 资料库联网结果展示 | 前端闭环 M47：`api.ts` 新增 `webSearch()`/`webRead()` 客户端封装；资料库（`pages/Workspace.tsx`）顶部加「联网搜索」面板——输入 → `web.search` 渲染**真实**标题/链接/摘要，每条结果一个「读正文」按钮内联展开 `web.read` 正文（可收起），无蓝、暖白卡片 | ✅ 前端 build（tsc + vite）通过；后端注入 E2E 验证两条链路（web.search 命中 8 条真结果 + web.read 抽到标题/正文），全量 114 用例仍绿 |
 本地 webhook 网关 `/api/im`：**默认关闭**（settings 表 key-value，enabled/secret/name）；入站 POST 命中工作意图即入队真实任务，secret 充当鉴权（入站 POST 豁免 M36 登录门禁，配置 PUT 仍要登录）；前端 `/app/im` 配置页（开关/名称/secret 复制+轮换/本地 endpoint） | ✅ 3 store 单测（默认关+自动 secret/toggle+改名/轮换）+ E2E（默认关 404/门禁 401/开启/错 secret 403/建任务/非任务），全量 107 用例 |
 `tools/knowledge.ts` search.knowledge——零 embedding 的 RAG-lite：跨「记忆（searchMemoriesFor 高召回）+ 工作空间文件名（FTS/LIKE）」检索相关片段（source/kind/title/snippet），多用户隔离；注册进 `/api/tools` + `POST /api/tools/search.knowledge` | ✅ 3 knowledge 单测 + E2E（列表/检索/缺 query 400），全量 104 用例 |
 `tools/web.ts` 本地 fetch+去标签抽正文字本（http/https-only、限长截断、失败返回 error 不抛）；`/api/tools` 工具清单 + `POST /api/tools/web.read` 真调用入口 | ✅ 4 web 单测（协议/抽正文/title/截断/HTTP 错）+ E2E（列表/真实读取/协议拦截/缺 url），全量 101 用例 |
 `POST /api/chat` 命中 `/记得 …` 命令即存记忆并 SSE 回执 `memory_saved`；否则按相关度把用户记忆抽进上下文（`searchMemoriesFor` 抽 CJK 二元组+英文词 LIKE 高召回，优于 FTS 整句短语）；前端注入条数气泡 `memory_ctx` | ✅ 3 parseRemember 单测 + 3 store searchMemoriesFor 单测 + E2E（/记得 存 + 注入），全量 97 用例 |
 `memories` 表 + `memories_fts`（FTS5 trigram）CRUD/检索，多用户隔离；`/api/memories` 路由；前端 `/app/memories` 页（分类筛选/搜索/内联编辑/删除），无蓝、暖色偏好评级 pill | ✅ 4 store 单测 + E2E（建/列表/检索/改/删），全量 90 用例 |

## 二、正在开发什么

- **已刚完成（M24~M40 可靠性加固 + 治理 + 效率 + 门禁 + 版本化 + Agent 可配置 + 对话多轮）**：进程重启任务恢复（getUnfinishedTasks/resumeUnfinishedTasks）；vitest 自动化测试（M25 起，现 86 用例，揪出 insertTask 缺 archived 列、getUnfinishedTasks 忘 AS userId 两个真实 bug）；结构化日志（util/log.ts，taskId 贯穿）；多渠道熔断器（连续失败 5 次 open + 10s cooldown + half-open 探测，pickChannel/failover 跳过坏渠道）；验收回归评测集（M28）；操作审计日志（M29）；任务并发控制（M30）；逐步断点续跑（M31）；自动清理策略（M32）；任务模板（M33）；任务另存为模板（M34）；模板字段真正生效（M35）；认证硬门禁（M36）；定时任务可跑模板（M37）；**交付版本历史（M38：产物快照进 `_ark_versions/` + `file_versions` 表，/api/versions 列表与回滚）**；**Agent 角色可配置（M39：专家内置+用户自建落库 CRUD，技能页新建/删除，专家页「创建专属专家」可用）**；**对话式多轮任务执行（M40：对话里把消息跑成真实任务，SSE 回传 task_created，气泡内实时进度+交付下载）**；**记忆系统（M41：memories 表+memories_fts 全文检索，/api/memories 路由，前端记忆页分类/搜索/内联编辑，多用户隔离）**；**记忆注入对话+`/记得` 命令（M42：对话里 `/记得` 即存记忆回执；相关记忆自动抽进模型上下文，前端有注入气泡）**；**内置网页读取工具（M43：web.read 本地抽正文，/api/tools 工具清单+调用入口）**；**内置知识检索工具（M44：search.knowledge 跨记忆+工作空间 RAG-lite，多用户隔离）**；**IM 消息桥（M45：默认关闭的本地 webhook 网关，secret 鉴权，命中意图即入队任务，/app/im 配置页）**；**Web/检索工具接入编排管线（M46：gatherContext 采网址+本地知识，注入多 Agent LLM + 追加参考资料 section 进交付）**；**内置联网搜索工具（M47：search.web 本地优先免 key 走 DuckDuckGo HTML，可配 provider；调研/搜索类提示词自动联网注入标题摘要；/api/tools 清单+调用入口）**；**资料库联网结果展示（M48：api.ts 封装 webSearch/webRead；工作空间页「联网搜索」面板搜真实结果+点开读正文，前端闭环 M47）**。
- **正在做**：继续按路线图推进剩余进度（见 [5-企业级演进路线](5-企业级演进路线.md) 未勾选项）。

## 三、架构决策备忘（详见 8-变化原因）
- **后端**：Node 22 + TS + Fastify + 内置 `node:sqlite`（零编译，Windows 友好）。
- **编排**：先自写状态机，留 `plannerLLM` 接入点，按需演进（不提前上 LangGraph）。
- **进度推送**：SSE（单向持续推送 + replay 缓存）；M21 起有全局事件流（/api/events）供任意页通知。
- **LLM**：openai 兼容 fetch adapter，零依赖，DeepSeek/OpenAI/Ollama 通吃；M14 起 chatWithFailover 按成功率逐个试所有渠道，M15 加权（可靠性/延迟/成本/优先级），M27 加每个渠道熔断（5 次失败 open + 10s cooldown + half-open 探测）。
- **Office 交付**：pptxgenjs / exceljs / docx（全 JS，零原生编译），按需求关键词选类型，产出可编辑真实文件。
- **模型路由**：router 按渠道综合评分选优，统计写 SQLite；坏渠道自动降权 + 熔断短路。
- **验收/重试**：verifier 对交付文件做真实校验（存在/非空/zip），失败限次重试。
- **工作空间**：`GET /api/workspace` 真扫 `frontend/public/workspace/`，按类型/大小/时间列出；M20 起文件名 FTS5 trigram 全文搜索（Cmd+K 搜文件）。
- **对话流式**：`POST /api/chat` SSE 逐 token 推送；配 key 走真实模型 `chatStream`，无 key 走内置回复逐字流式降级；M19 起会话落库持久化、M23 起对话内容全文搜索。
- **自动化**：`scheduler/jobs.ts` 本地 Cron（jobs/job_logs 表），30s 轮询匹配「每天 HH:MM / every:N 分钟」到点调 runTask 并记日志；同分钟防重。M37 起 action 支持 `template:<id>` 定时跑模板（复用模板 config）。
- **模型渠道**：DB 持久化（channels 表，env 引导种子），CRUD + 验活（GET /models）+ 设默认；router/planner 直接消费 DB 渠道。
- **连接器**：`GET /api/connectors` 真实状态——渠道计数 + Ollama 探测（GET /api/tags），未接入的如实标未接入（不假在线）。
- **场景库**：`GET /api/scenarios`（`data/scenarios.ts` 6 场景），首页/提示词库共用；前端 `listScenarios` 拉取（mock 仅离线降级）。
- **任务执行可靠性（M16 起）**：M16 串行队列（runner.ts 模块级 Promise 链）→ M30 改信号量受控并发（默认同跑 2，ARK_MAX_CONCURRENCY 覆盖，FIFO 排队）+ 失败兜底（异常→failed+error，不卡 running）；M17 重试/删除；M24 进程重启恢复；M31 断点续跑（resumeTask）；M26 任务 ID 贯穿日志。
- **测试（M25 起）**：vitest（ARK_DB_PATH/ARK_TEST 隔离临时库，绝不碰真实 data/ark.db），store/events/runner/HTTP 10 文件 86 用例，npm test / typecheck(tsconfig.test.json) / build 三件套全绿。
- **认证（M36 起）**：写请求硬门禁（`routes/guard.ts` 根 preHandler：POST/PUT/PATCH/DELETE→`/api/*` 需登录 401，读开放；`/api/auth/*` 自管）；`ARK_REQUIRE_AUTH=0` 回退软门禁（本地单机/演示）。前端全部写调用已带 `authHeaders()`。
- **自动清理（M32 起）**：tasks.created_ts 存 epoch ms（created 是 zh-CN 本地化字符串不可排序）；`maintenance/cleanup.ts` 按龄删终态非归档任务的交付文件并从库清除，审计日志裁剪到 AUDIT_MAX；安全护栏 safeUnlink 只删工作目录内文件防路径穿越；`POST /api/maintenance/cleanup` 手动触发，调度器每 6h 自动，env 可开关/调留存。
- **任务模板（M33 起）**：`task_templates` 表（name/desc/prompt + 可选的 expert/skills/model）多用户隔离 CRUD + `POST /:id/run` 一键应用模板建任务（复用 enqueueTask 同管线）；前端 `/app/templates` 页（列表/新建/编辑/删除/应用并执行）。

## 四、文档索引（AGENTS/）
| 文档 | 内容 |
|---|---|
| [1-入门说明](1-入门说明-README.md) | 是什么、怎么跑、代码在哪 |
| [2-面试准备指南](2-面试准备指南.md) | 花式问答 + 硬技能清单 |
| [3-程序文件详解](3-程序文件详解.md) | 每个文件做什么、怎么协作 |
| [4-项目完整流程](4-项目完整流程.md) | 一句话 → 交付文件端到端 |
| [5-企业级演进路线](5-企业级演进路线.md) | 单机 → 企业级分阶段路线 |
| [6-架构技术栈图](6-架构技术栈图.md) | 分层架构图 + 选型原因 |
| [7-踩坑日志](7-踩坑日志.md) | 踩过的坑：现象→原因→解决 |
| [8-变化原因](8-变化原因.md) | 为什么从 A 变 B |
| [9-排查日志](9-排查日志.md) | 排查路径 / 方法 |

> 说明：`AGENTS/`（及本目录）是**跟踪入库**的权威版本；`docs/agent-architecture/` 下的同名文件是开发过程中的**工作副本**（gitignored、内容近似但未入库）。**以 `AGENTS/` 为准，且每完成一个里程碑都须同步 `AGENTS/` 再提交**。

